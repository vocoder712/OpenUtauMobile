<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="https://github.com/MahApps/IconPacks.Avalonia"
             xmlns:openUtauMobile="clr-namespace:OpenUtauMobile"
             xmlns:localTokens="clr-namespace:OpenUtauMobile.Controls.Tokens"
        xmlns:theme="clr-namespace:OpenUtauMobile.Themes.OpenUtauMobile.Tokens"
        xmlns:foundation="clr-namespace:OpenUtauMobile.Themes.OpenUtauMobile.Tokens.Foundation"
        x:Class="OpenUtauMobile.Controls.PianoRollEditModeSwitcher"
             HorizontalAlignment="Right">
    <UserControl.Styles>
        <StyleInclude Source="/Controls/Styles/EditModeSwitcher.axaml" />
    </UserControl.Styles>

    <Border x:Name="Capsule"
            Background="{DynamicResource Sem.Color.SurfaceContainer}"
            CornerRadius="{x:Static foundation:ShapeTokens.CornerXL}"
            Height="{x:Static localTokens:EditModeSwitcherTokens.ButtonSize}"
            ClipToBounds="True"
            HorizontalAlignment="Right">
        <Grid ColumnDefinitions="Auto,Auto"
              VerticalAlignment="Center"
              HorizontalAlignment="Right">
            <!-- 左侧编辑模式按钮列表 -->
            <Grid Grid.Column="0"
                  x:Name="ModeButtonsGrid"
                  ColumnDefinitions="Auto,Auto"
                  VerticalAlignment="Center"
                  ClipToBounds="True">
                <Grid.Transitions>
                    <Transitions>
                        <DoubleTransition Property="Width" Duration="{x:Static foundation:MotionTokens.DurationBase}" Easing="CubicEaseOut" />
                    </Transitions>
                </Grid.Transitions>
                <StackPanel Grid.Column="0"
                            x:Name="ModeButtonsPanel"
                            Orientation="Horizontal"
                            Spacing="{x:Static openUtauMobile:ViewConstants.EditModeItemSpacing}">
                    <Button Content="{iconPacks:PhosphorIcons Hand}"
                            Classes="EditModeBtn"
                            x:Name="HandButton"
                            Click="OnModeButtonClick" />
                    <Button Content="{iconPacks:PhosphorIcons Selection}"
                            Classes="EditModeBtn"
                            x:Name="MultiSelectButton"
                            Click="OnModeButtonClick" />
                    <Button Content="{iconPacks:PhosphorIcons MusicNoteSimple}"
                            Classes="EditModeBtn"
                            x:Name="NoteButton"
                            Click="OnModeButtonClick" />
                    <Button Content="{iconPacks:PhosphorIcons Pencil}"
                            Classes="EditModeBtn"
                            x:Name="PitchPenButton"
                            Click="OnModeButtonClick" />
                    <Button Content="{iconPacks:PhosphorIcons Cursor}"
                            Classes="EditModeBtn"
                            x:Name="AnchorButton"
                            Click="OnModeButtonClick" />
                    <Button Content="{iconPacks:PhosphorIcons WaveSine}"
                            Classes="EditModeBtn"
                            x:Name="VibratoButton"
                            Click="OnModeButtonClick" />
                    <!-- 分隔线 -->
                    <Border Width="1"
                            Background="{DynamicResource Sem.Color.Outline}" />
                </StackPanel>
            </Grid>
            <!-- 右侧当前编辑模式按钮 -->
            <Button Grid.Column="1"
                    x:Name="SwitchButton"
                    Classes="EditModeBtn"
                    Click="OnSwitchButtonClick"
                    ToolTip.Tip="{DynamicResource PianoRoll.EditMode.Switch}">
            </Button>
        </Grid>
    </Border>
</UserControl>